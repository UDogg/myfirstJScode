// this entire function will not be available to students they will create their own file
function changeColor() {
    // the input to this function will only be tested as a hex value without the # in front
    // any other case it should just go black
    // please write the code after this line
    document.getElementById("result").style = "color: #" + document.getElementById("hexinput").value ;

}